export const GRID_COLUMNS = 3;
export const GRID_ROWS = 3;
export const TOTAL_HOLES = GRID_COLUMNS * GRID_ROWS;

export const GAME_DURATION_SECONDS = 45;
export const SPAWN_INTERVAL_MS = 700;
export const HIT_POINTS = 1;

export type GameStatus = "idle" | "running" | "ended";
